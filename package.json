{
  "name": "switchyard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control panel for individually managed local services",
  "license": "MIT",
  "engines": {
    "node": ">=20.11"
  },
  "workspaces": [
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace @switchyard/web && npm run build --workspace @switchyard/server",
    "start": "node packages/server/dist/index.js",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "rm -rf packages/server/dist packages/web/dist"
  }
}
